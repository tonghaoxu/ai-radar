# AI Radar 全量审查与修复报告

审查日期：2026-09-05。范围覆盖应用页面、API、数据库、抓取器、初始化与维护脚本、依赖、启动配置和文档。修复未重置现有业务数据库，没有部署、提交或推送代码。

## 已修复的问题

| 范围           | 发现的问题                                                                             | 实施的修复                                                                                                                   |
| -------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| API 鉴权       | 同源 Origin 可以绕过密钥，收藏及已读接口未统一鉴权                                     | 所有写接口共用密钥校验，恒定时间比较；生产未配置密钥拒绝写入，本地开发仅允许本机非跨站请求                                   |
| 摘要接口       | 客户端可提交任意 URL，导致 SSRF 与不受控抓取；缺少数量限制、缓存                       | 仅接受 1–10 个已收录文章 ID，限制正文大小，摘要缓存 24 小时、请求锁及一分钟频率限制；只调用固定 AI 服务地址                  |
| 查询一致性     | 搜索提前返回，忽略分类、来源、日期及收藏；总数与列表使用不同条件                       | 列表、计数共用 WHERE 构造；论文搜索与分类、精选筛选可组合                                                                    |
| 输入校验       | 负数、NaN、巨大 limit 可能触发 SQL 错误或无限查询                                      | 所有目录统一分页整数边界，校验布尔值、日期、时区与搜索长度；无效输入返回 400                                                 |
| 字面搜索       | `%`、`_`、反斜杠转义位置不一致                                                         | 在数据库边界统一转义，支持中英文及字面特殊字符                                                                               |
| 日期与首页     | 使用 UTC 判断“今日”；抓取阻塞已有数据展示；旧资讯冒充今日                              | 浏览器时区参与日统计；先读取已有内容，最近资讯单独标注；输入时间规范化                                                       |
| 刷新与抓取     | 客户端多个页面可重复触发，失败可能当成成功                                             | 区分列表刷新和来源抓取，使用跨进程 SQLite 租约锁；逐源返回错误，处理量包含更新                                               |
| 论文           | 页面 GET 调用了只支持 POST 的接口；正则 XML 解析对命名空间不稳                         | POST 指定 papers 范围；Cheerio XML 解析；一次 OR 查询替代四路并发；新增记录规范 arXiv ID                                     |
| 模型           | 价格两位小数导致低价显示为零，未知价格当免费；变体 ID 碰撞；按厂商误标开源             | 保留价格精度和完整模型 ID，明确零值/未知；显示来源和更新时间，许可证缺失标注未核验                                           |
| 历史数据       | 硬编码模型、价格、跑分缺少可核验出处；初始化可能覆盖在线数据                           | 停止写入这些种子记录，历史条目明确标注；无来源跑分不展示；旧 OpenRouter ID 被接替后保留但隐藏重复                            |
| 数据库         | 每次启动重建全文索引，初始化失败可能留下半初始化单例                                   | 迁移版本控制、IMMEDIATE 事务、失败关闭连接、查询索引；已读/收藏不触发 FTS 内容重写                                           |
| 初始化与清理   | INSERT OR REPLACE 可能产生删除/重建语义，产品时间被重置；清理脚本默认直接删除          | 产品改用 UPSERT，保留时间与其他数据；默认仅预览清理，执行时保护已读和收藏                                                    |
| 前端数据       | 快速切换过滤条件可能被旧请求覆盖；失败静默显示空列表                                   | 公共读取 Hook 取消过期请求并限制超时，目录支持错误提示与重试                                                                 |
| 导航和使用逻辑 | 全站搜索实际只跳到资讯；目录固定首批；返回位置和缓存筛选易错配                         | 四类全站结果，各目录分页与搜索；资讯筛选进入 URL，缓存按完整查询匹配，返回恢复滚动                                           |
| 可访问性       | 图标按钮缺少名称、系统暗色首次切换无效、小屏顶部拥挤                                   | 补充 aria 标签、主题使用 resolvedTheme、响应式换行、跳到正文链接、减少动态效果偏好                                           |
| 工程与依赖     | ESLint 基线 58 个错误、12 个警告；依赖审计 14 项漏洞；未使用图表和庞大的 CLI 运行依赖  | 清理类型与 Hook 问题，升级 Next.js / ESLint 配套至 16.3.4；移除 recharts、uuid、shadcn CLI，保留本地样式；安全补丁后审计为 0 |
| 文档           | Node 18 要求已过时，刷新、XML、图表、种子行为描述陈旧；README 商业限制与 AGPL 文件冲突 | 更新 README、开发说明、环境模板与贡献检查，代码许可说明与现有 LICENSE 对齐                                                   |

新增统一类型、输入校验、API 错误处理、可重复的测试与格式化命令。没有通过关闭 lint 或类型规则掩盖问题。

## 验证

| 检查                              | 最终结果                     |
| --------------------------------- | ---------------------------- |
| Prettier 格式检查                 | 通过                         |
| ESLint（不允许警告）              | 0 错误、0 警告               |
| Next.js 路由类型生成 / TypeScript | 通过                         |
| 核心测试                          | 13 / 13 通过                 |
| Playwright 桌面 + 手机            | 18 / 18 通过                 |
| Next.js 生产构建                  | 通过，全部页面与接口完成编译 |
| npm 官方依赖审计                  | 0 已知漏洞（修复前 14 项）   |
| Git diff 空白检查                 | 通过，无冲突标记或空白错误   |

当前工作区的检查日志存放在 `.audit-work`，浏览器截图在 `test-results`，两者均不进入版本库。

- 核心回归：查询组合与计数、字面搜索、日期边界、UPSERT 状态保留、FTS 同步、输入边界、鉴权绕过、摘要约束和缓存、价格精度、XML 命名空间、重复初始化、并发锁、链接协议、关键词误报和旧数据库迁移。
- 浏览器回归：桌面及 375px 手机布局、主要页面、脚本错误、横向溢出、分页、组合筛选、收藏、详情返回、论文抓取、四类搜索、错误重试和系统主题切换。
- 测试在内存数据库或 `.e2e-work` 临时数据库执行。未使用真实 DeepSeek 密钥产生付费调用。

## 实际公开来源检查

2026-09-05 执行过一次实际抓取，写入独立审查数据库。它验证该次运行情况，不代表来源永久可用。

| 来源                  | 结果                                         |
| --------------------- | -------------------------------------------- |
| arXiv                 | 60 篇论文                                    |
| 量子位                | 7 篇资讯                                     |
| TechCrunch AI         | 11 篇资讯                                    |
| MIT Technology Review | 8 篇资讯                                     |
| Hacker News           | 11 篇资讯                                    |
| GitHub Trending       | 12 个项目                                    |
| OpenRouter            | 返回 431 个模型，本项目厂商筛选后写入 284 个 |
| 36氪                  | 响应无法按 RSS 解析                          |
| VentureBeat           | HTTP 429，来源限流                           |
| RSSHub 公共实例       | HTTP 403，访问限制                           |

上述处理数包含新增及更新。实际检查后，AI 专题 RSS 改为保留来源自身的专题筛选，避免标题未含关键词而漏收，因此后续数量可能不同。受限来源没有绕过访问控制；页面会展示失败。若需要稳定长期订阅，应取得可用的授权订阅源或自建合法可用的 RSSHub 服务。

## 保留的边界与后续建议

1. **公开多用户使用**：当前为个人实例。管理密钥解决写接口保护，但不提供用户隔离；公开前应增加登录、会话、每用户收藏与阅读状态。属于产品架构变更，未擅自替换。
2. **持续抓取**：自动刷新是浏览器读取列表。关闭浏览器后需要独立调度器调用命令行或 POST 接口；未在用户设备上擅自创建定时任务。后续可增加任务队列、逐源退避及运行历史。
3. **更好的信息组织**：建议增加按主题订阅、相关文章聚合、稍后阅读，以及来源可信度和内容新鲜度评分，优先解决重复事件造成的信息噪音。
4. **大量中文检索**：当前参数化 LIKE 正确支持中英文；数据达到较大规模时，建议实测后引入中文分词全文检索。现有 FTS 基础保留。
5. **模型及产品资料**：旧条目与静态产品资料不会自动变成已核验事实。建议增加编辑审核、来源证据和核验日期；目前通过明确历史标签和来源链接降低误用。
6. **AI 总结质量**：结构、缓存和错误路径有测试，真实付费模型输出未调用；上线后应使用少量真实样本核验摘要准确性。
7. **测试边界**：Chrome 桌面及手机尺寸已测，未覆盖所有浏览器、网络环境和未来来源变化。

## 参考

- [Next.js 2026 年 8 月安全更新说明](https://nextjs.org/blog/august-2026-security-release)：核验框架补丁背景，最终依赖以 npm 官方注册表和 lockfile 为准。
- [OpenRouter 模型接口](https://openrouter.ai/docs/api/api-reference/models/list-all-models-and-their-properties)：模型字段、每 token 价格及聚合目录语义。
- [GNU 许可证常见问题](https://www.gnu.org/licenses/gpl-faq.en.html)：README 的商业限制说明不应与既有自由软件许可证混用；本项目保留原 LICENSE 文本。
